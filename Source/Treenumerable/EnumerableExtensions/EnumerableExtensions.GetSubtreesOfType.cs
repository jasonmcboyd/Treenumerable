using System;
using System.Collections.Generic;

namespace Treenumerable
{
    public static partial class EnumerableExtensions
    {
        // TODO:
        //public static IEnumerable<TResult> GetSubtreesOfType<TNode, TWalker, TResult>(
        //    this IEnumerable<TNode> nodes, 
        //    ITreeWalker<TWalker> walker, 
        //    Func<TNode, bool> predicate, 
        //    bool includeNodes)
        //    where TWalker : TNode
        //{
        //    //return walker.GetSubtrees(nodes, predicate, includeNodes);
        //    walker.Get
        //}

        //public static IEnumerable<T> GetSubtreesOfType<T>(this IEnumerable<T> nodes, ITreeWalker<T> walker, Func<T, bool> predicate)
        //{
        //    return walker.GetSubtrees(nodes, predicate, false);
        //}
    }
}
